{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run OpenWeather (MAUI Windows)",
      "type": "node-terminal",
      "request": "launch",
      "command": "dotnet run --project .\\OpenWeather\\OpenWeather.csproj -f net8.0-windows10.0.19041.0 --launch-profile Terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
